# name: workflow name
# on: event that trigger workflow (workflow_dispatch is be trigger manually by user)
name: First Workflow
on: workflow_dispatch 
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello world!"
      - name: Print goodbye
        run: echo "done bye!"
      
